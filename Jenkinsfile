if (env.BRANCH_NAME == 'develop') {
    node {
        stage('Compile') {
            echo 'Compile'
        }
    }
} else {
    node {
        stage('Build') {
            echo 'Build'
        }
    }
}
